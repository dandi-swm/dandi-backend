services:
  app:
    image: ${ECR_IMAGE_URI:-nyummy-app:local}
    container_name: nyummy-app
    # env_file은 환경별 파일(local/dev)에서 지정한다.
    ports:
      - "8080:8080"
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

